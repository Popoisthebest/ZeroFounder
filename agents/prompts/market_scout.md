Extract recurring, concrete user problems. Propose only the specificity and directness classification for existing signal IDs. Code calculates recency, reliability, duplicate clusters, and final evidence scores.

